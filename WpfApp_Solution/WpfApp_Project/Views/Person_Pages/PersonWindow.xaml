<Window x:Class="WpfApp_Project.Views.Person_Pages.PersonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_Project.Views.Person_Pages"
        mc:Ignorable="d"
        Title="PersonWindow" Height="450" Width="1280">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10">
                <Label Content="Filtrar por nome:"/>
                    <TextBox Width="100" Margin="5" Text="{Binding FilterName}" />
                <Label Content=" Filtrar por CPF:"/>
                    <TextBox Width="100" Margin="5" Text="{Binding FilterCPF}" />
            </StackPanel>

            <DataGrid x:Name="dataGridPersons" ItemsSource="{Binding Persons}" AutoGenerateColumns="False" Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200"/>
                    <DataGridTextColumn Header="CPF" Binding="{Binding CPF, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                    <DataGridTextColumn Header="Address" Binding="{Binding Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100"/>


                    <DataGridTemplateColumn Header="Ações" Width="250">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Editar" Width="60" Margin="5" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}">
                                    </Button>
                                    <Button Content="Excluir" Width="60" Margin="5" Command="{Binding DataContext.ExcludeCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}">
                                    </Button>
                                    <Button Content="Incluir Pedido" Width="100" Margin="5" Command="{Binding DataContext.SelectPersonCommand, RelativeSource={RelativeSource AncestorType=Window}}"  CommandParameter="{Binding}">
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>


            <Button Content="Incluir Pessoa" Command="{Binding SaveCommand}" Margin="10" Width="100" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</Window>
